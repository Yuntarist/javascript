let boolean = prompt("true또는 false를 입력하세요")
        let list = "";
        console.log(typeof(list));
        list+=`<h1>논리 연산자</h1>`;
        list+=`<h2>&&연산</h2>`;
        list+=`<table class="a">`;
            list+=`<tr>`;
                list+=`<th>&&연산</th>`;
                list+=`<th>연산</th>`;
                list+=`<th>값</th>`;
                list+=`<th>결과</th>`;
            list+=`</tr>`;
            list+=`<tr>`;
                list+=`<td>${boolean}</td>`;
                list+=`<td>&&</td>`;
                list+=`<td>false</td>`;
                list+=`<td>${false&&boolean}</td>`;
            list+=`</tr>`;
            list+=`<tr>`;
                list+=`<td>${boolean}</td>`;
                list+=`<td>&&</td>`;
                list+=`<td>true</td>`;
                list+=`<td>${true&&boolean}</td>`;
            list+=`</tr>`;
        list+=`</table>`;
        list+=`<h2>||연산</h2>`;
        list+=`<table class="b">`;
            list+=`<tr>`;
                list+=`<th>||연산</th>`;
                list+=`<th>연산</th>`;
                list+=`<th>값</th>`;
                list+=`<th>결과</th>`;
            list+=`</tr>`;
            list+=`<tr>`;
                list+=`<td>${boolean}</td>`;
                list+=`<td>||</td>`;
                list+=`<td>false</td>`;
                list+=`<td>${false||boolean}</td>`;
            list+=`</tr>`;
            list+=`<tr>`;
                list+=`<td>${boolean}</td>`;
                list+=`<td>||</td>`;
                list+=`<td>true</td>`;
                list+=`<td>${true||boolean}</td>`;
            list+=`</tr>`;
            list+=`</table>`;
        document.write(list);